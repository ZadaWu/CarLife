{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://deploy.local/deploy.catalog.schema.json",
  "title": "deploy script catalog record",
  "description": "infra/deploy.catalog.jsonl 里一行一份部署脚本（infra/<infra>/sprints/M<n>-<NN>-<slug>.sh）的登记记录。唯一真相源是脚本头部的 `# key: value` 元数据；除 tags 外全部字段由 `内部技能模板 register` 从头部派生，source_hash = sha256(脚本原文)，改了脚本就要重新 register。运行记录不在这里——在 infra/deploy.runs.jsonl（deploy.runs.schema.json）。各 infra/<infra>/README.md 的索引表由本台账 + 运行台账生成（deploy.mjs index）。",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version", "catalog_id", "entity_type", "entity_id", "infra", "sprint", "seq", "slug",
    "title", "title_en", "status", "services", "envs", "destructive", "workorder",
    "tags", "source_path", "source_hash", "registered_at", "updated_at"
  ],
  "properties": {
    "schema_version": { "const": "1.0.0" },
    "catalog_id": { "const": "deploy" },
    "entity_type": { "const": "deploy-script" },
    "entity_id": { "type": "string", "pattern": "^(docker|k8s|host)/M[0-9]+-[0-9]{2}$", "description": "<infra>/M<n>-<NN>；同一设施同一 Sprint 内 NN 递增，一经分配不复用。" },
    "infra": { "enum": ["docker", "k8s", "host"], "description": "部署设施 = infra/ 下的一级目录。" },
    "sprint": { "type": "string", "pattern": "^M[0-9]+$" },
    "seq": { "type": "integer", "minimum": 1, "maximum": 99 },
    "slug": { "type": "string", "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$", "minLength": 4, "maxLength": 60, "description": "kebab-case 英文动宾短语，说「对部署做了什么」；与文件名一致。" },
    "title": { "type": "string", "minLength": 2, "maxLength": 120 },
    "title_en": { "type": "string", "minLength": 2, "maxLength": 120 },
    "status": { "enum": ["draft", "ready", "applied", "retired"], "description": "draft 还在写；ready 过了 check 可以跑；applied 至少在一个环境跑过 pass；retired 被后续脚本覆盖、不再跑（文件保留）。只前进。" },
    "services": { "type": "array", "minItems": 1, "uniqueItems": true, "items": { "type": "string", "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$" }, "description": "头部 services：preflight 逐个核对存在 / 运行。" },
    "envs": { "type": "array", "minItems": 1, "uniqueItems": true, "items": { "enum": ["dev", "test", "prod"] }, "description": "头部 envs：允许跑的环境；没声明的环境 common.sh 拒绝。" },
    "destructive": { "type": "boolean", "description": "头部 destructive：正文含删除 / 不可逆动作（每处都必须 destructive 包裹，check 守）。" },
    "workorder": { "type": ["string", "null"], "pattern": "^M[0-9]+-[0-9]{2}$", "description": "对应的施工单（内部文档），没有就 null。" },
    "tags": { "type": "array", "minItems": 2, "uniqueItems": true, "items": { "type": "string", "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$" } },
    "source_path": { "type": "string", "pattern": "^infra/(docker|k8s|host)/sprints/M[0-9]+-[0-9]{2}-[a-z0-9]+(-[a-z0-9]+)*\\.sh$" },
    "source_hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "registered_at": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$" },
    "updated_at": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$" }
  }
}
