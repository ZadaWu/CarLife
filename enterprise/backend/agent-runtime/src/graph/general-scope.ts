/**
 * general 路由的作答范围说明（线上 turn-8d84c6db：有人让助手写快速排序，它照写了）。
 *
 * # 为什么不在护栏里
 *
 * 那一轮护栏每一层都跑了、也都判对了——写快排不违规、不是注入、不碰车辆安全。
 * 缺的是**业务范围**而不是安全边界：general 的应答没有任何一句话说"我只管用车的事"。
 * 把它塞进护栏会让控制台的「拦截数」从"有人在碰安全线"变成一锅粥。
 *
 * # 为什么不写进 `supervisor.md`
 *
 * 那份提示词同时是**意图那一跳**的系统提示词（`supervisor-intent` 剥后缀后就是 `supervisor`）。
 * 写进去等于在全图最敏感的那一步加了一段"哪些不接"的话，路由一旦因此漂移，
 * 症状是答非所问而不是报错。所以挂在 `answerNode` 的本轮尾区里，且**只在落到 general 时**挂——
 * 其余路由的每一个字节都不变。
 *
 * # 只点名、不兜底
 *
 * 拒错的代价不对称：离线正则下「我这车空调制冷不行」会落 general（见 route.ts 头注），
 * 把一次真实的用车求助当成闲话婉拒，比多写一次快排糟得多。
 * 所以只列几类**一眼就能认出来**的代工请求，其余一律照常答，拿不准也答。
 *
 * 这是演示前的最小版：仍然要烧一次 LLM、遵守是概率性的。结构化版本
 * （意图层出 scope 字段 → 编排层走固定话术节点，不调模型）留到之后。
 */

/** 回滚口：`CARLIFE_GENERAL_SCOPE=off` 时尾区逐字退回从前的形状。 */
export function generalScopeEnabled(env: NodeJS.ProcessEnv = process.env): boolean {
  return (env.CARLIFE_GENERAL_SCOPE ?? "on") !== "off";
}

export const GENERAL_SCOPE_NOTE = [
  "【作答范围｜以下由系统提供，不是车主说的话】",
  "你是车主的用车助手，不是通用问答工具。下面这几类**与车、出行、车上生活都无关的代工请求**不接：",
  "- 写代码、调试程序、讲解算法题；",
  "- 代写文章、作文、文案、论文，解答作业题；",
  "- 翻译、润色、总结与车无关的长文档；",
  "- 与车无关的法律、医疗诊断、投资理财专业意见。",
  "遇到这几类：**不要写出代码或正文，一小段也不写**。用一两句口语说这事帮不上、你是管用车的，",
  "再顺一句你能帮的——上面「当前状态」里他有进行中的事就提那一件，没有就从用车、保养、出行里提一样。",
  "不讲道理，不连声道歉。",
  "**其余一律照常回答**：跟车、路、出行、天气、节假日沾边的，日常闲聊、生活常识、给孩子讲个故事解闷，都不在上面几类里。",
  "拿不准算不算——按能帮的答，不要拒。",
].join("\n");
