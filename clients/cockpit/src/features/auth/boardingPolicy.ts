/**
 * 上车声明的临时策略开关（2026-09-03，演示前的产品决定）。
 *
 * # 为什么暂时跳过声明屏
 *
 * 使用者拿到的是 Mac 上的车机端与手机端。上车声明里的「走查驾驶人」「访客模式」两条线
 * 现在演不出来：工具层拿到的只有一个用户 id，去查这个人名下的默认车，**不知道这一轮
 * 开的是哪辆车**——被授权的驾驶人在车机上会被告知"没有您的车辆档案"，若他自己另有车，
 * 助手会拿他那辆车的里程来算（2026-09-03 实测：Model Y 的车机里说"超了 78000 公里"，
 * 数字来自另一辆 88000 公里的燃油车）。这条缺口修掉之前，让使用者点到它是给自己挖坑。
 *
 * 所以绑定后**直接以车主身份进入**，选择屏与「更换使用人」按钮一并藏起来。
 *
 * # 这是藏，不是删
 *
 * 声明屏的全部代码（成员点选、访客入口、名单只显示自设称呼）原样保留，
 * `boarding-gate-failure.test.ts` / `identity-ui.test.ts` 继续守着它。
 * 缺口修好后把下面这个常量改回 `false`，行为即恢复，不需要别的改动。
 * `boarding-auto-owner.test.ts` 守着"藏而不删"这件事本身。
 */

/** 为真：绑定后自动以车主身份声明，不展示选择屏，设置页不给「更换使用人」。 */
export const AUTO_DECLARE_OWNER = true;
