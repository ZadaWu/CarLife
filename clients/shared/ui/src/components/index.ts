/**
 * clients/shared/ui/src/components — 跨端通用组件（尚未实现）。
 *
 * ⚠️ 这个目录是空的，但**不要删**：它是 4 个功能点指定的落点，其中 2 个是 P0。
 *
 * # 与同级目录的分工
 *
 * `clients/shared/ui` 下已有内容的是**场景化**目录——`hud/`、`map/`、`assistant-avatar/`、
 * `themes/`、`hooks/`，它们各自服务一个具体场景。本目录放的是**跨场景复用**的通用件
 * （弹窗、折叠面板、详情卡），所以它不在 `src/index.ts` 的导出里：
 * 空目录导出一个空对象，只会让"组件库里有通用组件"这件事看起来成立。
 *
 * 补齐时记得**同时**把 `export * from "./components"` 加进 `src/index.ts`，
 * 否则组件写好了消费方仍然 import 不到。
 *
 * # 待落地的功能点
 *
 * | 功能点 | 要求 |
 * |---|---|
 * | F-04-01 (P0) | 跨层确认弹窗组件，层级高于 HUD 与对话层（overlay/portal，不依赖路由层级） |
 * | F-04-02 (P0) | 动作明细渲染——**不只显示动作名**；与 `appointment` 的 `describeDisclosure()` 对接，逐项列出将外发的信息 |
 * | F-03-04 | 思考过程折叠展开 |
 * | F-03-05 | 方案详情卡片展开 |
 */

export {};
