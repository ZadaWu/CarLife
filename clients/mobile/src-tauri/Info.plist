<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- 走查 2026-08-29 ②：macOS 麦克风用途声明（push-to-talk 采集）。
     cockpit 早在 M2-03 就有这份，手机端一直缺——缺它的后果不是"不弹框"，
     而是调用 requestAccessForMediaType 时进程被 TCC **直接杀掉**。
     除麦克风与定位外不声明任何其它敏感权限（§8.5 能力最小化）。

     定位（本次新增）：**默认停用**，且端上另有一道自己的开关与粒度选择
     （模糊 / 精确，见 `clients/shared/rust/carlife-core/src/location.rs`）。系统权限拿到手
     不等于会去用它——不授权就一次都不发起。这条声明与 §8.5 能力最小化不冲突：
     少了它，调用系统定位时进程会被 TCC 直接杀掉（与麦克风那条同一种坏法）。 -->
<plist version="1.0">
<dict>
  <key>NSLocationWhenInUseUsageDescription</key>
  <string>用于把地图停在你附近、并找到附近的充电桩、加油站与服务门店。可以在「设置 › 定位」里随时停用，也可以只授予模糊定位。</string>
  <!-- 本地网络（M65-03）：手机经 Wi-Fi 访问 Mac 上的网关是局域网单播，iOS 14+ 要先拿到这项授权。
       此前这条只写在 gitignored 的 gen/apple 模板里，仓库里没有；NSBonjourServices 让授权框的文案
       带上服务名，也是 mDNS 探测（src/local_network.rs）能被系统计入的前提。 -->
  <key>NSLocalNetworkUsageDescription</key>
  <string>用于连接您局域网内运行 CarLife 服务的电脑（网关），获取行程、车辆与对话数据。</string>
  <key>NSBonjourServices</key>
  <array>
    <string>_http._tcp</string>
    <string>_carlife._tcp</string>
  </array>
  <key>NSMicrophoneUsageDescription</key>
  <string>按住助手说话时录制您的语音指令；开启设置页的「语音唤醒」后，会持续监听以识别唤醒词「暖暖」（未唤醒的语音判定后即弃）。该开关默认关闭。</string>
  <!-- 相机与相册（M104-06）：拍照问诊的快门是 `<input type=file accept="image/*" capture="environment">`，
       系统相机由 WebView 代为拉起——**代拉起也算本进程在用相机**，少了这条声明，真机上点快门的表现是
       「App 直接退出」（2026-09-18 iPhone 16 Pro Max 实测），与文件头记的麦克风那次是同一种坏法：
       不是不弹框，是 TCC 把进程杀掉，端上什么日志都留不下。
       相册那条：不带 capture 的同款 input 在 iOS 上弹的 action sheet 里**同样有「拍照」**，
       且系统图库选择器（PHPicker）虽是跨进程、理论上不要授权，仍按最小声明原则一并写明用途。 -->
  <key>NSCameraUsageDescription</key>
  <string>用于「拍照问诊」：拍下仪表警示灯、轮胎、漏液或车身的照片，交给助手对照车主手册判断。照片随这一轮提问上传，不做人脸识别、不长期留存在本机。</string>
  <key>NSPhotoLibraryUsageDescription</key>
  <string>用于从相册里选一张已经拍好的车辆照片交给助手判断。只读取你本次选中的那一张，不扫描整个相册。</string>
</dict>
</plist>
