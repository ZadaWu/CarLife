/**
 * features/service — 售后服务 界面（尚未实现）。
 *
 * ⚠️ 这个文件是占位，但**不要删**：它是 FL-20 指定的落点。
 * 待落地：故障描述、维修下单、保养提醒。
 *
 * A3 交付的是"最小可跑"：HUD 首页 + 对话层 + HITL 确认弹窗 + 语音上行，
 * 即一条完整的「看见 → 说话 → 得到回复 → 确认有后果的动作」链路。
 * 五个业务 Agent 各自的专属界面不在这一轮——它们依赖各自 Agent 的
 * 结构化输出契约，而那些契约在服务端还没定型。
 */
export default function Placeholder() {
  return null;
}
